# Minutos entre regeneraciones automáticas del snapshot
REFRESH_INTERVAL_MINUTES=60

# Token opcional para proteger POST /refresh (déjalo vacío para no exigir auth)
REFRESH_TOKEN=

# Parámetros de normalización (deben coincidir con el entrenamiento del modelo)
NORM_TEMP_MIN=0
NORM_TEMP_MAX=40
NORM_VIENTO_MAX=100
NORM_PRESION_MIN=980
NORM_PRESION_MAX=1040
NORM_PRECIP_MAX=50
